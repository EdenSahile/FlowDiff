# Handoff — 2026-06-03 18:40

## Feature active
EDI — mise aux normes production EDIFACT : ORDRSP en cours (template mis à jour, non commité)

## Objectif de la session
Continuer l'alignement des templates EDIFACT sur les vrais messages production (suite de la session ORDERS).  
L'utilisateur a aussi modifié et commité lui-même le template INVOIC entre deux sessions.

---

## Ce qui a été fait

### Commité en début de session (`d694afb`)
- `handoffs/2026-06-03-1630-handoff.md` + `CONTEXT.md` mis à jour

### Modifié par l'utilisateur (déjà commité, hors session Claude)
- `src/lib/ediUtils.ts` — template INVOIC entièrement réécrit : UNA, UNH docRef + EAN008, NAD BY/SU avec BUYER_GLN/SUPPLIER_GLN, RFF+API, CUX:3, PIA+5+EAN:SA, IMD auteur/titre via helpers, QTY+47, MOA+146/203, PRI+AAB, CNT+1, MOA+79/9, UNT docRef, UNZ docRef
- `src/pages/edi/__tests__/ediUtils.test.ts` — test INVOIC détaillé ajouté (lignes 174-206) : vérifie UNA, UNH, BGM+380, NAD, RFF+API, CUX:3, RFF+ON, PIA:SA, IMD, QTY+47, MOA+146/203, PRI+AAB, CNT, MOA+79/9, UNZ

### En cours cette session — NON commité
- `src/lib/ediUtils.ts` — template ORDRSP réécrit (session interrompue avant commit) :
  - `UNA:+.? '` ajouté en tête
  - UNB : `SUPPLIER_GLN:14` + `BUYER_GLN:14` + `msg.documentRef` comme interchange ref
  - `UNH+${msg.documentRef}+ORDRSP:D:96A:UN:EAN008'`
  - BGM status dynamique : 29=accepté, 27=rejeté, 4=partiel
  - `NAD+SE+${SUPPLIER_GLN}::9'` + `NAD+BY+${BUYER_GLN}::9'`
  - `CUX+2:EUR:9'`
  - Par ligne : `PIA+5+EAN:IB'` + `IMD+L+050+:::titre'` via `titleToImd050()`
  - `CNT+1:{nbLignes}'` + `CNT+2:{qtyConfirmée totale}'`
  - `UNT+N+${msg.documentRef}'` + `UNZ+1+${msg.documentRef}'`
  - **Tests ORDRSP non mis à jour** (edit rejeté par l'utilisateur lors de l'interruption)

---

## État du build
- **TypeScript** : clean (0 erreur) — y compris avec les changements ORDRSP non commités
- **Tests** : 180/181 passants — le test ORDRSP existant (`BGM+231`) passe encore (l'ancien test est compatible)
- **CONTEXT.md** : non mis à jour cette session
- **Git** : `src/lib/ediUtils.ts` modifié, non stagé — à commiter en priorité

---

## Blocages / échecs
- L'édition du fichier de test pour ORDRSP a été interrompue par l'utilisateur → les nouveaux tests ORDRSP ne sont pas écrits

---

## Prochaines étapes (ordre strict)

### 1. Vérifier et commiter ORDRSP (priorité immédiate)
```bash
npx tsc --noEmit   # doit être clean
npx vitest run     # 180/181 attendu
git add src/lib/ediUtils.ts
git commit -m "feat(edi): template ORDRSP production — UNA, GLN placeholders, UNH/UNT docRef, CUX:9, PIA, IMD titre, CNT, BGM status dynamique"
```

### 2. Ajouter les tests ORDRSP dans `src/pages/edi/__tests__/ediUtils.test.ts`
Remplacer le test `'ORDRSP contient BGM+231'` par un test complet vérifiant :
- `UNA:+.? '` en tête
- `UNB+UNOC:3+DiffuseurGLN:14+ClientGLN:14+`
- `UNH+{docRef}+ORDRSP:D:96A:UN:EAN008'`
- `BGM+231+{docRef}+29'` pour ACCEPTED, `+27'` pour REJECTED, `+4'` pour PARTIAL
- `RFF+ON:{orderId}'`
- `NAD+SE+DiffuseurGLN::9'` + `NAD+BY+ClientGLN::9'`
- `CUX+2:EUR:9'`
- `PIA+5+EAN:IB'` par ligne
- `IMD+L+050+:::titre'` par ligne
- `QTY+83:{backorderQty}'` si reliquat
- `DTM+358:YYYYMMDD:102'` si date estimée
- `CNT+1:{N}'` + `CNT+2:{qtyConfirmée}'`
- `UNT+N+{docRef}'` + `UNZ+1+{docRef}'`

### 3. Traiter DESADV
Même démarche : identifier les segments manquants dans `EDIFACT_TEMPLATES.DESADV` (ediUtils.ts), proposer à l'utilisateur, implémenter.  
DESADV a encore : vieux GLNs, `UNH+1+`, pas de UNA, pas de PIA/IMD, pas de CNT.

---

## Notes importantes
- `SUPPLIER_GLN = 'DiffuseurGLN'` et `BUYER_GLN = 'ClientGLN'` — constantes dans `ediUtils.ts`
- Dans ORDRSP, l'expéditeur (SE) = SUPPLIER_GLN (diffuseur), le destinataire (BY) = BUYER_GLN (libraire) — logique inversée par rapport à ORDERS
- `RFF+API` dans INVOIC utilise `BUYER_GLN` comme placeholder (cohérent avec le compte client)
- INVOIC utilise `CUX+2:EUR:3'` (qualificateur 3 = devise de la transaction) — différent de ORDERS/ORDRSP qui utilisent 9
