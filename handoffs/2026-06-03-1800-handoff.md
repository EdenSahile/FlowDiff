# Handoff — 2026-06-03 18:00

## Feature active
Refonte header (icônes droite) + corrections tableau EDI + renommage numérotation commandes EDI

## Objectif de la session
1. Corriger la page blanche en prod (TDZ dans OrdersContext)
2. Améliorer l'ergonomie des icônes header (notifications, listes, panier)
3. Corriger plusieurs bugs dans le tableau EDI (colonne Diffuseur, N° commande, onglet Factures)
4. Mettre à jour la numérotation des commandes EDI vers le nouveau format CMD0000XXX

---

## Ce qui a été fait (commité et pushé)

### Commit 1 — `fix(orders): corrige TDZ`
- `src/contexts/OrdersContext.tsx` — constantes `ORDER_COUNTER_KEY` et `ORDER_COUNTER_INITIAL` déplacées **avant** `migrateStorage()` (elles étaient déclarées après → ReferenceError → page blanche en prod)

### Commit 2 — `fix(header): StickyBand + padding vertical + refonte icônes droite`
- `src/components/layout/AppLayout.tsx` — `<StickyBand>` wrapping Header + DemoBanner (unique bloc sticky `top:0`) pour que le bandeau démo reste collé au header au scroll
- `src/components/ui/DemoBanner.tsx` — suppression `position: sticky` et `top` indépendants
- `src/lib/theme.ts` — `headerHeight: '56px'` → `'64px'`
- `src/components/layout/Header.tsx` — suppression `HelpBtn` (icône "?") ; `NavSeparator` entre NotifBell et les boutons ; `ListsBtn` carré 38px icône seule (s'élargit en pill "Listes N" quand `$hasLists`) ; `CartBtn` hauteur 34→38px ; gap corrigé (≥8px partout) ; `ListsLabel` visible seulement quand listes présentes
- `src/components/layout/NotificationBell.tsx` — `BellBtn` 34→36px
- `src/components/layout/Sidebar.tsx` — entrée `{ to: '/aide', label: 'Aide' }` ajoutée dans `infoItems`
- `src/pages/cart/CartPage.tsx` — `top` du sticky RecapCol : `headerHeight + demoBannerHeight + 16px`
- `src/pages/catalogue/FicheProduitPage.tsx` — `top: 94px` → `calc(headerHeight + demoBannerHeight + 4px)`

---

## Ce qui est en cours (NON commité — 5 fichiers modifiés)

### A. `src/components/edi/DesadvGroupedList.tsx`
- Colonne "Diffuseur" supprimée (en-tête + cellule groupe + cellule non-rattachés + `colSpan` 6→5)

### B. `src/pages/edi/EDIPage.tsx`
- Helper `getOrderRef(msg)` ajouté : pour ORDRSP, retourne `payload.orderId` (le vrai CMD) au lieu de `documentRef` (le ACK ref)
- Colonne "N° commande" → "N° facture" quand `activeFilter === 'INVOIC'`
- Cellule N° affiche `msg.documentRef` (INV-...) pour INVOIC, `getOrderRef(msg)` pour les autres
- Placeholder barre recherche : `"Rechercher par N° facture ou N° commande"` quand INVOIC
- Placeholder panneau latéral "Rechercher une commande" : `"Ex. CMD0000001"` (nouveau format)
- Export CSV utilise `getOrderRef(m)` au lieu de `m.documentRef`

### C. `src/lib/ediUtils.ts`
- `INVOICLine` interface ajoutée : `{ ean, title, qty, unitPriceTTC }`
- `INVOICPayload` mise à jour : champ `lines: INVOICLine[]` ajouté
- `messageContainsISBN` : branche INVOIC étendue — cherche dans `orderIds` ET dans `lines[].ean`

### D. `src/data/mockEDIMessages.ts`
- INVOIC `INV-2026-0422-001` : `orderIds` + `lines` (3 ISBN)
- INVOIC `INV-2026-0427-001` : `orderIds` + `lines` (2 ISBN)
- INVOIC `INV-2026-0430-001` (nouveau, multi-commandes) : `orderIds: ['CMD-2026-0426-001', 'CMD-2026-0426-002']` + `lines` (3 ISBN)

### E. État du build
- **TypeScript : clean** (vérifié juste avant handoff)
- Tests : non relancés cette session
- CONTEXT.md : non mis à jour cette session

---

## Tâche interrompue — Renommage numérotation EDI (à faire en priorité)

La session a été interrompue pendant cette tâche. **Rien n'a encore été modifié** pour le renommage.

### Mapping complet à appliquer

| Ancien ID (mockEDIMessages.ts) | Date | Nouveau numéro |
|-------------------------------|------|----------------|
| `CMD-2026-0422-001` | 2026-04-22 | `CMD0000001` |
| `CMD-2026-0424-001` | 2026-04-24T11:00 | `CMD0000002` |
| `CMD-2026-0424-002` | 2026-04-24T14:30 | `CMD0000003` |
| `CMD-2026-0425-001` | 2026-04-25T16:00 | `CMD0000004` |
| `CMD-2026-0426-003` | 2026-04-26T09:45 | `CMD0000005` |
| `CMD-2026-0426-002` | 2026-04-26T11:02 | `CMD0000006` |
| `CMD-2026-0426-001` | 2026-04-26T14:32 | `CMD0000007` |
| `CMD-2026-0427-001` | 2026-04-27T07:00 | `CMD0000008` |
| `CMD-2026-0503-001` | 2026-05-03 | `CMD0000009` |
| `CMD-2026-0504-001` | 2026-05-04 | `CMD0000010` |
| `CMD-2026-0505-001` | 2026-05-05 | `CMD0000011` |
| `CMD-2026-0508-001` | 2026-05-08 | `CMD0000012` |

| Ancien (mockOrders.ts) | Nouvelle date | Nouveau numéro |
|------------------------|---------------|----------------|
| `CMD0000001` (date: 2025-03-18) | 2026-05-25 | `CMD0000013` |
| `CMD0000002` (date: 2026-05-20) | 2026-05-28 | `CMD0000014` |

### Fichiers à modifier pour le renommage

1. **`src/data/mockEDIMessages.ts`** — remplacer toutes les occurrences avec `replace_all: true` dans cet ordre (du plus long au plus court pour éviter les collisions) :
   - `'CMD-2026-0426-003'` → `'CMD0000005'`
   - `'CMD-2026-0426-002'` → `'CMD0000006'`
   - `'CMD-2026-0426-001'` → `'CMD0000007'`
   - `'CMD-2026-0427-001'` → `'CMD0000008'`
   - `'CMD-2026-0424-001'` → `'CMD0000002'`
   - `'CMD-2026-0424-002'` → `'CMD0000003'`
   - `'CMD-2026-0425-001'` → `'CMD0000004'`
   - `'CMD-2026-0422-001'` → `'CMD0000001'`
   - `'CMD-2026-0503-001'` → `'CMD0000009'`
   - `'CMD-2026-0504-001'` → `'CMD0000010'`
   - `'CMD-2026-0505-001'` → `'CMD0000011'`
   - `'CMD-2026-0508-001'` → `'CMD0000012'`

2. **`src/data/mockOrders.ts`**
   - `numero: 'CMD0000001'` → `'CMD0000013'`, `date: '2025-03-18'` → `'2026-05-25'`
   - `numero: 'CMD0000002'` → `'CMD0000014'`, `date: '2026-05-20'` → `'2026-05-28'`

3. **`src/contexts/OrdersContext.tsx`**
   - `ORDER_COUNTER_INITIAL = 2` → `14` (les 14 commandes mock sont réservées)
   - `STORAGE_VERSION = '2'` → `'3'` (force migration localStorage en prod)
   - Mettre à jour le commentaire : `// CMD0000001 à CMD0000014 réservés aux mocks`

4. **`src/contexts/EDIContext.tsx`** — corriger la génération des refs ACK/DESADV pour le nouveau format :
   - Ligne ~121 : `order.numero.replace('CMD-', '')` → `order.numero.replace('CMD', '')`
   - Ligne ~136 : idem
   - Résultat : `ACK-0000015`, `DESADV-0000015` au lieu de `ACK-CMD0000015`

5. **Vérification finale** : `npx tsc --noEmit && npx vitest run`

6. **Commit groupé** (tous les fichiers en attente + le renommage) puis push

---

## Prochaines étapes (ordre strict)

1. Commiter d'abord les 5 fichiers déjà modifiés (EDI fixes) avec le message `fix(edi): supprime colonne Diffuseur, corrige N° commande ORDRSP, onglet Factures avec ISBN`
2. Faire le renommage numérotation (étapes 1→4 ci-dessus)
3. `npx tsc --noEmit` + `npx vitest run`
4. Commiter + pusher avec message `feat(edi): renumerotation commandes CMD0000001-CMD0000014, compteur mis à jour`
5. Mettre à jour CONTEXT.md
