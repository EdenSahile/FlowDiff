# Handoff — 2026-06-03 22:00

## Feature active
Références de commande sur les ORDERS EDI — implémentation à démarrer

## Objectif de la session
Brainstormer et planifier la feature "références de commande" : permettre au libraire d'associer une référence interne (globale ou par article) à sa commande, incluse dans le segment EDIFACT `RFF+CR` et affichée dans l'historique.

---

## Ce qui a été fait

### Fichiers créés
- `docs/superpowers/specs/2026-06-03-references-commande-edi-design.md` — spec complète validée par l'utilisateur (deux modes : global + par article, placement dans l'étape Panier, badge discret dans HistoriquePage)
- `docs/superpowers/plans/2026-06-03-references-commande-edi.md` — plan d'implémentation complet en 7 tâches avec code exact

### Commits de la session
- `docs: spec références commande EDI (global + par article)` — 46a51aa
- `docs: plan implémentation références commande EDI (7 tasks)` — 5fb4322

### Aucun code métier modifié — tout reste à implémenter

---

## État du build
- **TypeScript** : clean (hérité de la session précédente — aucun code modifié)
- **Tests** : 173/174 passants — 1 failing pré-existant ignoré (`useDashboardConfig.test.ts > has 3 bottomPanels all visible`)
- **CONTEXT.md** : à jour (session en cours vide — feature non démarrée)
- **Git** : tout commité, rien en attente

---

## Blocages / échecs
RAS — session de planification uniquement, aucun blocage.

---

## Prochaines étapes

Lire le plan complet avant de commencer : `docs/superpowers/plans/2026-06-03-references-commande-edi.md`

L'utilisateur veut **valider après chaque tâche** — ne pas enchaîner sans confirmation.

### Task 1 — Types (src/data/mockOrders.ts + src/lib/ediUtils.ts)

Ajouter :
- `referenceLigne?: string` sur `OrderItem` (mockOrders.ts ~ligne 54)
- `referenceCommande?: string` sur `Order` (mockOrders.ts ~ligne 77)
- `referenceLigne?: string` sur `ORDERSLine` (ediUtils.ts ligne 11)
- `referenceGlobale?: string` sur `ORDERSPayload` (ediUtils.ts ligne 16)

Puis : `npx tsc --noEmit` → commit.

### Task 2 — Mock data (src/data/mockOrders.ts)

Ajouter `referenceCommande: 'BC-2026-0412'` sur la commande `CMD0000013` (id: 'ord-001') pour que le badge soit visible en dev.

Puis : `npx tsc --noEmit` → commit.

### Task 3 — Template EDIFACT ORDERS (src/lib/ediUtils.ts lignes 278-298)

Remplacer le bloc `ORDERS: (msg) => {` pour ajouter :
- `...(p.referenceGlobale ? [\`RFF+CR:${p.referenceGlobale}'\`] : [])` dans le tableau `header` (après les NAD)
- `...(line.referenceLigne ? [\`RFF+CR:${line.referenceLigne}'\`] : [])` dans `lineSegments.flatMap` (après `QTY+21`)

La formule `untCount = header.length + lineSegments.length + 1` reste correcte (elle s'adapte automatiquement).

Puis : `npx tsc --noEmit` → commit.

### Task 4 — OrdersContext (src/contexts/OrdersContext.tsx)

Étendre l'interface `OrdersContextValue.addOrder` et la fonction `addOrder` avec :
- `referenceCommande?: string`
- `referencesParLigne?: Record<string, string>`

Dans `const orderItems`, ajouter sur chaque item :
```ts
referenceLigne: params.referencesParLigne
  ? (params.referencesParLigne[ebookOption ? `${book.id}::${ebookOption.isbnEbook}` : book.id]?.trim() || undefined)
  : undefined,
```

Dans `const order`, ajouter : `referenceCommande: params.referenceCommande`

Puis : `npx tsc --noEmit` → commit.

### Task 5 — CartPage (src/pages/cart/CartPage.tsx)

**5a — Styled components** à insérer après `const DatePickerLabel` (~ligne 576), avant le bloc `ÉTATS VIDE / SUCCÈS` :
`RefCard`, `RefOptional`, `RefHint`, `RefToggle`, `RefToggleBtn` (prop `$active: boolean`), `RefInput`, `RefLineList`, `RefLineRow`, `RefLineLabel`

**5b — State** à ajouter dans `CartPage()` après `const [saveAsDefault, ...]` (~ligne 1107) :
```ts
const [refMode, setRefMode]           = useState<'global' | 'par-ligne'>('global')
const [refGlobale, setRefGlobale]     = useState('')
const [refsParLigne, setRefsParLigne] = useState<Record<string, string>>({})
```

**5c — JSX** à insérer dans la vue panier principale (return à ligne 1441), après les OP groups (`</Section>` ~ligne 1686) et avant `{/* ── Livraison ── */}` (~ligne 1688) :
```tsx
{items.length > 0 && (
  <Section>
    <RefCard>
      <SectionTitle style={{ marginBottom: 4 }}>
        Référence commande<RefOptional>(optionnel)</RefOptional>
      </SectionTitle>
      <RefHint>Associez une référence interne (BC, rayon, projet…) incluse dans le message EDI.</RefHint>
      <RefToggle>
        <RefToggleBtn $active={refMode === 'global'} onClick={() => setRefMode('global')}>Référence globale</RefToggleBtn>
        <RefToggleBtn $active={refMode === 'par-ligne'} onClick={() => setRefMode('par-ligne')}>Par article</RefToggleBtn>
      </RefToggle>
      {refMode === 'global' ? (
        <RefInput type="text" maxLength={35} placeholder="Ex : BC-2026-0412, Rayon-Littérature…"
          value={refGlobale} onChange={e => setRefGlobale(e.target.value)} />
      ) : (
        <RefLineList>
          {items.map(item => {
            const key = getItemKey(item)
            return (
              <RefLineRow key={key}>
                <RefLineLabel>{item.book.title}</RefLineLabel>
                <RefInput type="text" maxLength={35} placeholder="Référence pour ce titre…"
                  value={refsParLigne[key] ?? ''} onChange={e => setRefsParLigne(prev => ({ ...prev, [key]: e.target.value }))} />
              </RefLineRow>
            )
          })}
        </RefLineList>
      )}
    </RefCard>
  </Section>
)}
```

**5d — handleConfirmOrder** (ligne 1164) : ajouter `referenceCommande` et `referencesParLigne` dans l'appel `addOrder(...)`.

Puis : `npx tsc --noEmit` → commit.

### Task 6 — HistoriquePage (src/pages/historique/HistoriquePage.tsx)

Ajouter `const RefBadge` après `const OrderDate` (~ligne 165) :
```ts
const RefBadge = styled.span`
  display: inline-block;
  margin-top: 4px;
  padding: 2px 8px;
  border-radius: 10px;
  background: ${({ theme }) => theme.colors.navyLight};
  color: ${({ theme }) => theme.colors.navy};
  font-family: ${({ theme }) => theme.typography.fontFamily};
  font-size: 11px;
  font-weight: ${({ theme }) => theme.typography.weights.semibold};
`
```

Dans la vue de la liste (~ligne 797), après `<OrderNumero>{order.numero}</OrderNumero>` :
```tsx
{order.referenceCommande && <RefBadge>Réf : {order.referenceCommande}</RefBadge>}
```

Puis : `npx tsc --noEmit` → commit.

### Task 7 — Vérification finale

```bash
npx tsc --noEmit
npx vitest run
```

Attendu : 0 erreur TS · 173/174 tests.

Mettre à jour `CONTEXT.md` : ajouter la feature terminée dans "État du build".
